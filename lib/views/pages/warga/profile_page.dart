import 'package:flutter/material.dart';

class WargaProfilePage extends StatelessWidget {
  const WargaProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Test Profile Page Warga"),
    );
  }
}