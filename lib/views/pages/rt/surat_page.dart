import 'package:flutter/material.dart';

class RtSuratPage extends StatelessWidget {
  const RtSuratPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Test Surat Page RT"),
    );
  }
}