import 'package:flutter/material.dart';

class RtHomePage extends StatelessWidget {
  const RtHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Test Home Page RT"),
    );
  }
}