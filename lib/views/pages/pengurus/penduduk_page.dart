import 'package:flutter/material.dart';

class PendudukPage extends StatelessWidget {
  const PendudukPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Test Penduduk Page"),
    );
  }
}