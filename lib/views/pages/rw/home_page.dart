import 'package:flutter/material.dart';

class RwHomePage extends StatelessWidget {
  const RwHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Test Home Page RW"),
    );
  }
}